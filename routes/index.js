const HOME_ROUTES = require('./home_routes');
const DASHBOARD_ROUTES = require('./dashboard_routes');
const INST_ROUTES = require('./inst_routes');
const API_ROUTES = require('./api');

module.exports = {
	HOME_ROUTES,
	DASHBOARD_ROUTES,
	INST_ROUTES,
	API_ROUTES
};
